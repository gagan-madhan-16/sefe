export const SubHeading = ({ label }) => {
    return (
      <div
        style={{
          color: "#6b7280",
          fontSize: "1rem",
          paddingTop: "0.25rem",
          paddingBottom: "1rem",
          paddingLeft: "1rem",
        }}
      >
        {label}
      </div>
    );
  };
  